[env]
# ts-rs writes every exported type here, so the whole IPC contract lands in one
# place the frontend imports from. Regenerated by `cargo test`; CI fails if the
# result differs from what is committed.
TS_RS_EXPORT_DIR = { value = "src/bindings", relative = true }
